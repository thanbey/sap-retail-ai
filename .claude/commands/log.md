Append a note to today's build log.

Determine today's day number N from the README.md status table (the first row not marked done) or ask me if unclear. The file is docs/log/day-NN.md (two digits). If it does not exist, create it with a first line stating the day's deliverable from docs/work-plan.md.

Append exactly one line in the form `- HH:MM  $ARGUMENTS` using the current local time. Do not rewrite or reformat existing lines. Confirm with the line you added and nothing else.
